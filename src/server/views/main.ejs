<!DOCTYPE html>
<html lang="<%= locale %>">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--<link rel="icon" href="<%=App.config.urlPrefix%>/favicon.ico" type="image/x-icon"/>
    <link rel="shortcut icon" href="<%=App.config.urlPrefix%>/favicon.ico" type="image/x-icon"/>-->
    <link rel="icon" href="/halloween25_icon.png" type="image/png" />
    <link rel="shortcut icon" href="/halloween25_icon.png" type="image/png"/>

    <!-- Bootstrap -->
    <link href="<%=App.config.urlPrefix%>/theme/<%= App.config.theme %>.min.css" rel="stylesheet">
    <link href="<%=App.config.urlPrefix%>/theme/htw.css" rel="stylesheet">

    <script src="/htw.js"></script>

    <title><%= title %></title>
  </head>
  <body>
    <div class="container"><% if (user) { %>
        <%- include('./statusBar', {user, t, prefix: App.config.urlPrefix, App}) %>
      <% } %>
      <header class="page-header">
        <h1><a href="<%=App.config.urlPrefix%>/"><%= brand %></a></h1>
        <p class="lead"><%= t('home.slogan') %></p>
      </header>
      
      <hr class="my-4"/>
      
      <% if (heading) { %>
        <h2><%= heading %></h2>
      <% } %>
      
      <% if (backButton) { %><p style="margin-top: 24px; margin-bottom:48px;"><a href="<%=App.config.urlPrefix%><%-backHref%>" id="back-button"><%= t('challenge.back') %></a></p><% } %>
      <% const contentHtml = content ? content : include(pagePath, {props, t, user, App, prefix: App.config.urlPrefix}) %>
    <% if (!outsideOfContainer) { %>
      <%- contentHtml %>
      <% if (backButton) { %><div style="height: 100px;"></div><% } %>
    </div>
    <% } else { %>
    </div>
    <%- contentHtml %>
    <% } %>
  </body>
</html>
 
